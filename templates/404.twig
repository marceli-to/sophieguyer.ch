{% extends '/partials/layout.twig' %}
{% block content %}
    {% include '/partials/navigation.twig' %}

    <main  class="animate-elements">
        <div class="container main-title-container">
            <section class="main-title">
                <h1>Seite nicht gefunden</h1>
            </section>
        </div>

        <div class="container mt-90 visible" style="min-height: 400px;">
            <section class="text" id="abschnitt-943">
                <h2 class="h2 title"></h2>
                <div class="redactor-text">
                    <p>Die von Ihnen gesuchte Seite existiert leider nicht.</p>
                    <p>&nbsp;</p>
                    <ul class="downloads-list list">
                        <li class="download-item">
                            <a href="/" title="Startseite" class="button button-download">
                                <span class="button-text">Zur Startseite</span>
                            </a>
                        </li>
                    </ul>
                </div>
            </section>
        </div>

    </main>

    {% include '/partials/footer.twig' %}

{% endblock %}
